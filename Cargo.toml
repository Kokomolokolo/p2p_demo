[package]
name = "p2p_demo"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.17"
bevy_ggrs = { version = "0.19", features = ["wasm-bindgen"] }
bevy_matchbox = { version = "0.13", features = ["ggrs"] }

[profile.dev.package."*"]
opt-level = 2